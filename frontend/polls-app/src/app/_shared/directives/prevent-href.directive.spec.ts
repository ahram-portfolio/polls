import { PreventHrefDirective } from './prevent-href.directive';

describe('PreventHrefDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventHrefDirective();
    expect(directive).toBeTruthy();
  });
});
